import makeStyles from "@mui/styles/makeStyles";

export const useStyles = makeStyles(() => {
  return {
    header: {
      display: "flex",
      height: 50,
      backgroundColor: "green",
    },
  };
});
